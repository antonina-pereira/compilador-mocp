/*Chamada de Funções com Múltiplos Parâmetros*/
inteiro multiplicar(inteiro, inteiro);

inteiro multiplicar(inteiro x, inteiro y) {
    retornar x * y;
}

vazio principal() {
    inteiro base = 4;
    inteiro altura = 5;
    inteiro area = multiplicar(base, altura);
}