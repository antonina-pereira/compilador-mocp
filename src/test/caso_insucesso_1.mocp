/* Exemplo com insucesso */
/* Retorna x ou 0 apesar da funcção 'principal' retornar vazio */
inteiro x = 10;
real pi = 3.14;

vazio principal() {
    se (x > 5) {
        retornar x;
    } senao {
        retornar 0;
    }
}
