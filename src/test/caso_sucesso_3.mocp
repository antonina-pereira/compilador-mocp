/*Lógica Booleana e Negação*/
vazio principal() {
    inteiro idade = 20;
    inteiro tem_bilhete = 1;

    se (idade >= 18 && tem_bilhete == 1) {
        idade = 21; /* Pode entrar */
    } senao {
        se (!tem_bilhete) {
            idade = 0; /* Vai comprar bilhete */
        }
    }
}